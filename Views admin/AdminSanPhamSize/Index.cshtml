@model IEnumerable<ShopOnline.Models.SanPhamSize>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Quản lý sản phẩm size</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SanPham.Ma)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Size.MaSize)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SanPham.Ma)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Size.MaSize)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SanPhamSizeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SanPhamSizeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SanPhamSizeID })
        </td>
    </tr>
}

</table>
