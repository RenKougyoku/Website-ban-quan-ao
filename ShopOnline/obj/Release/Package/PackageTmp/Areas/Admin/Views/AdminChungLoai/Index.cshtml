@model IEnumerable<ShopOnline.Models.ChungLoai>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Quản lý chủng loại</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ten)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BiDanh)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ten)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BiDanh)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ChungLoaiID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ChungLoaiID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ChungLoaiID })
        </td>
    </tr>
}

</table>
